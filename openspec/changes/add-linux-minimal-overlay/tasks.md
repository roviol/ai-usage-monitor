## 1. Monitor geometry for Linux

- [x] 1.1 In `src/ui/overlay_frame.cpp`, add a non-Windows branch that builds the same `MonitorData`-shaped list (id, monitor rect, work rect, primary flag) used by `ResolveMonitor`/`AnchorToSavedCorner`/`SnapToNearestCorner`, using `wxDisplay::GetCount()`, `GetGeometry()`, `GetClientArea()`, `IsPrimary()`.
- [x] 1.2 Derive the monitor "id" from `wxDisplay::GetName()`, falling back to `"display-<index>"` when the name is empty, so `SelectOverlayMonitor` (unchanged, in `overlay.cpp`) keeps working.
- [x] 1.3 Implement a Linux `ResolveMonitor(requested)` and `MonitorAtWindow()`-equivalent (find the display under the overlay's current position) using the above.

## 2. Linux `MinimalOverlayFrame` implementation

- [x] 2.1 In `src/ui/ui.h`, widen the `MinimalOverlayFrame` class declaration guard from `#ifdef _WIN32` to `#if defined(_WIN32) || defined(__linux__)`, keeping `HotkeyAvailable()`/`HotkeyWarning()`/`MSWWindowProc` and hotkey/foreground-hook members under a nested `#ifdef _WIN32`.
- [x] 2.2 In `src/ui/overlay_frame.cpp`, add an `#elif defined(__linux__)` branch implementing the constructor: same `wxFrame` style flags (`wxBORDER_NONE | wxFRAME_NO_TASKBAR | wxSTAY_ON_TOP`), same `ApplyApplicationIcon`/`SetBackgroundStyle`/`SetName`, bind paint/mouse/timer/display-changed handlers, skip hotkey registration and foreground hooks entirely.
- [x] 2.3 Implement `OnPaint` for Linux reusing the same drawing logic as the Windows routine (rows from `ProjectOverlayRows`, theme via `ResolveTheme`), calling into the exact same helper calls — duplicate the routine per design.md decision 1, not a shared abstraction.
- [x] 2.4 Implement `ReprojectAndResize`/`AnchorToSavedCorner`/`SnapToNearestCorner` for Linux: same sizing math (calls into `OverlayRowCapacity`/`ProjectOverlayRows`/`NearestOverlayCorner`, all unchanged in `overlay.cpp`), positioning via `Move()`/`SetSize()` instead of `SetWindowPos`.
- [x] 2.5 Implement `ShowOverlay`/`HideOverlay`/`ApplySettings` for Linux using `Show()`/`Hide()`/`Raise()` instead of `ShowWindow`/`SetWindowPos(HWND_TOPMOST...)`.
- [x] 2.6 Implement opacity via `wxTopLevelWindow::SetTransparent()` (hover-to-95%-then-fade using the same `ApplyOpacity(bool hovered)` shape as Windows); no-op safely (fully opaque) when `SetTransparent` returns `false`.
- [x] 2.7 Implement mouse drag-to-reposition (`OnMouseDown`/`OnMouseMove`/`OnMouseUp`) for Linux using `ClientToScreen`/`GetPosition()`/`Move()` (portable wx calls) instead of `SetWindowPos`; on release, call `SnapToNearestCorner()`.
- [x] 2.8 Implement `ScheduleCountdown`/`OnCountdown`/`OnDisplayChanged`/`PersistSettings`/`Shutdown` for Linux — identical shape to Windows, no Win32 calls involved.
- [x] 2.9 Skip rounded-window-region clipping on Linux (no `SetWindowRgn` equivalent); the painted background is already a rounded rectangle, so this is a cosmetic-only difference (square outer window, rounded fill).
- [x] 2.10 Do not implement `ToggleLock`, click-through, hotkey registration, or foreground/fullscreen suppression for Linux — omit those code paths per the MVP scope.

## 3. Wiring: app, tray, settings

- [x] 3.1 In `src/ui/app.cpp`, widen the guards around `overlay_` member use, `ApplyOverlaySettings`, `ToggleOverlayVisibility`, the `PublishSnapshots` overlay update, and the `ShutdownRuntime`/`ExitApplication` overlay teardown from `#ifdef _WIN32` to `#if defined(_WIN32) || defined(__linux__)`. Leave `ToggleOverlayLock`, `OnQueryEndSession`, and the hotkey-status lookup in `OpenSettings` strictly under `#ifdef _WIN32`.
- [x] 3.2 In `src/ui/tray_icon.cpp`, widen the overlay menu guard to include Linux, but only add the show/hide item (`ToggleOverlayVisibility`) for Linux — keep the lock item (`ToggleOverlayLock`) strictly Windows-only. Also widened `app.cpp`'s startup `ApplyOverlaySettings()` call and the `TrayIcon` constructor's overlay callback wiring to Linux (needed so a previously-enabled overlay comes back at launch, not only after re-opening Settings).
- [x] 3.3 In `src/ui/settings_dialog.cpp`, split the overlay panel: a shared block (Enable, Show, Opacity, Corner) built under `#if defined(_WIN32) || defined(__linux__)`, and a nested Windows-only block (Lock checkbox, full-screen-suppression checkbox, hotkey status text) under `#ifdef _WIN32`. Update `OnAccept`'s field-reading code the same way. Also widened the corresponding field declarations in `ui.h`.

## 4. Build and manual verification (Linux)

- [x] 4.1 Rebuild (`cmake --build --preset linux-release --parallel`) and fix any compile errors under `-Wall -Wextra -Wpedantic -Werror`. (One fix needed: `TrayIcon::VoidCallback{}` instead of a bare `{}` for the omitted lock-toggle callback — `std::make_unique`'s variadic forwarding can't deduce a type from an untyped empty brace-init-list.)
- [x] 4.2 Run `ctest --preset linux-release` and confirm the existing suite still passes unmodified (overlay engine tests in `tests/test_main.cpp` are platform-independent and should be unaffected). (2/2 passed.)
- [x] 4.3 Launch the app, enable the overlay from Settings, and confirm it appears anchored top-right with live snapshot rows matching the dashboard. (Verified via `AI_USAGE_UI_OVERLAY=1` fixture launch: overlay appeared top-right, same rows/values as the dashboard cards.)
- [x] 4.4 Drag the overlay to a different corner, release, and confirm it snaps there; restart the app and confirm the position persists. (Dragged to bottom-left via `xdotool`; snapped cleanly; `settings.json` recorded `corner: bottom-left`, `monitor: display-0`; restarted the process and the overlay came back at the exact same pixel position.)
- [x] 4.5 Change opacity in Settings and confirm the live overlay updates; hover the overlay and confirm it brightens, then fades back on mouse-leave. (Switched to a real, non-fixture launch — discovered `codex`/`claude` executables on this machine gave valid provider config, avoiding an unrelated pre-existing validation rule that fixture-mode data trips regardless of this change. Bumped opacity 78→88 in Settings and saved: `settings.json` recorded `opacity: 88`, and the live overlay window was destroyed/recreated with the new value with no crash. Hovered the live overlay window in/out: `OnMouseEnter`/`OnMouseLeave` → `ApplyOpacity`/`SetTransparent` run with no crash; this session's XFCE compositor is disabled [`xfconf-query -c xfwm4 -p /general/use_compositing` → `false`], so `SetTransparent` safely no-ops to fully opaque — exactly the degradation documented in design.md and the `linux-minimal-overlay` spec's "Platform does not support window transparency" scenario, observed for real rather than simulated.)
- [x] 4.6 Toggle Show/Hide from the tray menu and confirm the overlay follows without affecting the dashboard or provider refresh state. (Tray menu showed exactly `Abrir dashboard`/`Refrescar todo`/`Ocultar overlay`/`Configuración`/`Salir` — no lock item. Clicked `Ocultar overlay`: window went `IsUnMapped`, dashboard kept rendering normally. Menu label flipped to `Mostrar overlay`; clicked it and the window came back `IsViewable`.)
- [x] 4.7 Disable the overlay in Settings and confirm the window is removed and the app continues normally (tray + dashboard). (Unchecked `Habilitar` and saved: `settings.json` recorded `enabled: false`, the overlay window disappeared from the X window tree, and the process kept running normally with just the tray icon.)
- [x] 4.8 Restart the app with the overlay left enabled and confirm it comes back up in the previously persisted state (visible/hidden, corner, monitor, opacity). (Restarted with `enabled: true`, `corner: top-right`, `opacity: 88` in `settings.json`: the overlay reappeared automatically at the correct top-right position with live data and no crash — no Settings interaction needed.)

## 5. Windows regression check (code review only — no Windows machine available here)

- [x] 5.1 Re-read every touched Windows-only code path (`ui.h`, `app.cpp`, `tray_icon.cpp`, `settings_dialog.cpp`, `overlay_frame.cpp`'s `#ifdef _WIN32` branch) end to end to confirm none of the widened guards or nested-`_WIN32` splits changed Windows behavior or field layout. (`git diff` reviewed line by line for all 5 files: `overlay_frame.cpp`'s Windows implementation is a pure addition after — zero lines inside the original `#ifdef _WIN32 ... #endif` block were touched, only its closing `#endif` became `#elif defined(__linux__)` followed by new content. In `ui.h`/`app.cpp`/`tray_icon.cpp`/`settings_dialog.cpp`, every widened `#if defined(_WIN32) || defined(__linux__)` still evaluates true under `_WIN32`, and every nested nested-`_WIN32` nested block still includes exactly the same Windows-only declarations/fields/UI controls as before — only reordered relative to sibling code, which has no functional effect in C++.)
- [x] 5.2 Confirm `MinimalOverlayFrame`'s public interface (constructor signature, `ApplySettings`, `ShowOverlay`/`HideOverlay`, `SetSnapshots`) is unchanged for the Windows branch, so `app.cpp` call sites work identically on both platforms without platform-specific call code beyond the existing `#ifdef` splits. (Confirmed: those five members are declared once, unconditionally shared between both platforms in `ui.h`; only the Windows-only extras — `ToggleLock`, `HandleForegroundEvent`, `IsLocked`, `HotkeyAvailable`, `HotkeyWarning`, `MSWWindowProc` — sit behind the nested `_WIN32` guard, and every call site touching those is already itself Windows-only in `app.cpp`.)

## 6. Docs

- [x] 6.1 Add a short "Overlay" section to `docs/linux.md` describing how to enable it, its default corner, and the existing Wayland/GNOME "always on top may be ignored" caveat (already partially present) plus the click-through/hotkey/full-screen-suppression scope note from the proposal.
